php:
 - '(include|require_once|require)\s?\(?\s?(?:[\w]+code)?\(?(\$_GET\[.+?\]|\$_POST\[.+?\]|\$_REQUEST\[.+?\])\)?.+?\)?'
 - '(include|require_once|require)\s?\(?\s?(?:[\w]+code)?\(?.+?\b(MARK)\b.+?\)?.+?\)?'
 - '(include|require_once|require)\s?\(?\s?(?:[\w]+code)?\(?(\$_GET\[.+?\]|\$_POST\[.+?\]|\$_REQUEST\[.+?\])\)?.+?\)?'
 - '(include|require_once|require)\s?\(?\s?(?:[\w]+code)?\(?.+?\b(MARK)\b.+?\)?.+?\)?'
jsp:
 - '(include)\s+?page=(request\.getParameter\(.+?\))' 
asp:
 - '(response\.write)\((Request\.QueryString\(.*\))\)'
